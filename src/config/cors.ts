// CORS configuration for production use
// Update these origins based on your actual frontend domains
export const ALLOWED_ORIGINS = [
  // Add your frontend domains here
  // 'https://your-app.com',
  // 'https://app.your-domain.com',
  // For local development only:
  // 'http://localhost:3000',
]

// If you need to allow all origins (not recommended for production),
// you can use ['*'], but it's better to specify exact domains
